/**
 * Orders API
 *
 * Handles storefront checkout and order lookup.
 */

import * as mock from '../mocks/handlers'

export const createOrder = mock.createOrder
export const lookupOrderByReference = mock.lookupOrderByReference
